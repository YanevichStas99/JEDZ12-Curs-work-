package com.company;

import javafx.scene.canvas.GraphicsContext;

public class Groupe extends Figgure {
    public Groupe(GraphicsContext gc, double x, double y, int count) {
        super(gc, x, y, count);
    }

    @Override
    public void draw() {

    }
}
